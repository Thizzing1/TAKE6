import "./style.css";

export const BorrowButton = ({ className }) => {
  return (
    <div className={`borrow-button ${className}`}>
      <div className="text-wrapper-4">Borrow</div>
    </div>
  );
};

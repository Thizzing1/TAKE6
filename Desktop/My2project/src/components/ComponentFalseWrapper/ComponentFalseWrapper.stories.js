import { ComponentFalseWrapper } from ".";

export default {
  title: "Components/ComponentFalseWrapper",
  component: ComponentFalseWrapper,
};

export const Default = {
  args: {
    className: {},
  },
};

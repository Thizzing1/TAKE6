import "./style.css";

export const ConnectwalletButton = ({ className }) => {
  return (
    <div className={`connectwallet-button ${className}`}>
      <div className="text-wrapper-9">Connect wallet</div>
    </div>
  );
};
